package com.example.pomokingdom

import android.os.Bundle
import androidx.fragment.app.Fragment
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.recyclerview.widget.LinearLayoutManager
import androidx.recyclerview.widget.RecyclerView

class FriendsFragment : Fragment() {

   // private var layoutManager: RecyclerView.LayoutManager? = null
   // private var adapter: RecyclerView.Adapter<FriendsAdapter.FriendsViewHolder>? = null


    fun onCreateView(inflater: LayoutInflater, container: ViewGroup, savedInstanceState: Bundle) {
        super.onCreateView(inflater,container,savedInstanceState)

     //   layoutManager = LinearLayoutManager(this)
     //    recyclerViewFriend.LayoutManager = layoutManager

     //   adapter = FriendsAdapter()
     //   recyclerViewFriend.adapter = adapter
    }
}